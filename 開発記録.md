## 新宿御苑シーン 川の流れ実装・メッシュ追従化（2026-07-04）

### 川が流れない問題の修正（River Source をSimゾーン内へ）
- 症状: Main Water System（無限オーシャン Y=-5）の「広がった海」だけが見え、モデルの谷筋に川が生成されない
- 原因: 唯一の水源 River Source が Y=-5 で、River Sim Zone（Y範囲 -0.7〜7.3）の外側（真下）にあり、シミュ領域内へ注水できていなかった
- 対応: River Source を (32.7, -5, 11.59) → (32.7, 0, 11.59) へ移動（Y=0はWaterStartと同じ谷筋高さ・ゾーン内）→ 谷筋に川が生成された

### 水量調整・ドレイン追加・ゾーン延長
- River Source `ConstantFlowRate`: 4 → 1.5 → 0.75 に段階削減
- 出口側 (-90, 0, 11.59) に **River Drain** 新規追加（KWS.KWS_DynamicWavesObject / InteractionType=WaterDrain / ForceType=Box / ConstantDrainRate=1.0 / スケール4×6×12で谷筋幅をまたぐ）
- River Sim Zone スケールX 105 → 156（中心 -16.95 は不変のまま対称延長。下流端 -69.45 → -94.95 でモデル末端 -94.5 に到達）

### 方式変更: 箱底でなくメッシュ形状に沿う流れへ
- Shinjuku固有の **WaterChannelBox**（99.3×2×12 のメッシュ箱・Layer0）が深度ベイクに焼き込まれ、その平らな箱底（Y=0）に水が溜まって隆起した帯状になっていた（他の川シーン RiverTonnnelMap=MainRiver / MurkyTileMap=BigRiver 等には箱は存在しない）
- WaterChannelBox を無効化（ベイク対象から除外）→ River Sim Zone をトグルして深度を再ベイク → 水がモデルの岩の谷筋形状に沿って薄く流れるようになった

### 復旧用MCPツール追加（ActivateInactiveMCPTool）
- 問題: 標準の `SetGameObjectActive` は `GameObject.Find` を使い、一度無効化したオブジェクト（非アクティブ）を再検出できず有効化できない。深度再ベイクのため River Sim Zone を無効化した際に戻せなくなった
- 対応: `SetActiveIncludeInactive`（`scene.GetRootGameObjects()` で非アクティブなルートも再帰探索して SetActive）を新規追加・`AllMCPToolsBuilder` に登録 → River Sim Zone を復旧

---

## カメラ手挙げ分岐選択・寄り道ブラックアウト演出・新宿御苑シーン整理（2026-06-18）

### カメラ（手挙げ）での分岐選択（RouteBranchChooser）
- 分岐UI表示中のみ、MediaPipe Pose の手挙げを検知して選択を確定
- 右手挙げ=まっすぐ / 左手挙げ=脇道（`swapLeftRight`で入替可）。`holdTime`(0.7s)保持で確定（誤爆防止）、`raiseThreshold`調整可
- 既存 `PoseLandmarkProvider`（腕振り検知と同じ）を使用。姿勢データ無し時は無反応でマウス併用可。Play実機で動作確認済み（左手→脇道遷移）

### 寄り道のブラックアウト演出（RouteFollower）
- 移動前: `NewWayPoint` を少し注視 → ブラックアウト(`detourFadeDuration`) → 黒幕下でカット → フェードイン
- 復帰前: `NewLookPoint(1)` を少し注視 → ブラックアウト → 元の場所へカット・向き復元 → フェードイン → 待機 → 再開
- 既存 `FadeManager`（自動検索）使用。`detourGazeTime`/`detourFadeDuration` で調整

### 脇道遷移先シーンの整理
- `ShinjukuJGixyoenRiverMapTest`（TestScenes）→ `Assets/Scenes/ShinjukuGyoenMap.unity` へ移動・改名（GUID保持・ビルド設定自動追従）
- RouteBranchChooser.detourSceneName を `ShinjukuGyoenMap` に更新

### HerdSim Rat AnimatorController 警告除去
- 「AnyState→各state に条件もExit Timeも無い」遷移6件（無視される無効遷移）を削除し警告解消（外部アセット直編集・HerdSim再インポートで再発）

### 新規MCPツール
- BuildSettingsMCPTool に MoveAsset（AssetDatabase.MoveAsset・GUID保持移動）/ RemoveSceneFromBuildSettings 追加
- AnimatorCleanupMCPTool（RemoveInvalidAnyStateTransitions）

---

## ルート寄り道・最終地点分岐選択・外部カメラ・各種修正（2026-06-17）

### RouteFollower「寄り道（ルート中央離脱）機能」
- ルート全長の指定割合（既定50%＝空間的な真ん中）到達で発動（`detourTriggerProgress`、累積移動距離ベース）。インデックス中央ではなく空間中央
- フェーズ1: NewWayPointへ即カット切替（当たり判定無視の貫通移動を回避）／フェーズ2: NewWayPoint→NewWayPoint(1)を`detourMoveSpeed`(0.5)で移動しつつNewLookPoint間を補間注視／フェーズ3: 寄り道開始時のリグ位置・カメラ向きを厳密復元（同じ視界へ戻す）→`detourReturnWaitSeconds`(2s)待機→通常ルート再開
- 既存WayPoint注視演出との競合（isPerformingEvent二重制御で本筋移動が割り込む）を解消

### 最終地点での分岐選択（新規 RouteBranchChooser）
- ルート最終WayPoint到達時、自動シーン遷移の代わりにUI（旧UIボタン2個「まっすぐ進む」「脇道（新宿御苑）へ」）を表示
- まっすぐ＝そのシーンの通常遷移先（RouteFollowerが`targetSceneName`を渡す）／脇道＝`ShinjukuJGixyoenRiverMapTest`（新宿御苑モデル）
- 配置: 2シーン目 `RiverTonnnelMap` の出口（1シーン目MurkyTileMapには不要のため削除）
- 日本語フォント `BIZ-UDGothicR` 割当・サイズ34、Canvas sortingOrder=1000

### FadeManager クリック遮断バグ修正
- フェード用全画面Image（sortingOrder9999・最前面）の`raycastTarget`がtrueのままで、透明でも全UIクリックを遮断していた → Awakeで`fadeImage.raycastTarget=false`を設定

### 外部カメラ対応（CameraInput）
- `preferredDeviceNameKeyword`（既定"eMeet C960"）で外部カメラをデバイス名一致で優先選択（index並び順変動に頑健）

### ビルド設定 / フォント
- `ShinjukuJGixyoenRiverMapTest` をビルド設定に追加（脇道遷移先）
- `Assets/Fonts/BIZ-UDGothicR.ttc` 追加（旧UI日本語表示用）

### 新規/拡張 MCPツール
- ExportPackageMCPTool（依存込みUnityPackageエクスポート）/ WebCamDeviceMCPTool（WebCamデバイス列挙）/ BuildSettingsMCPTool（ビルド設定シーン追加・一覧）/ TextFontMCPTool（TextへのFontアセット・サイズ割当）/ CreateSceneMCPTool.LoadScene をフルパス・プロジェクト全体検索対応に拡張

### 備考
- MediaPipe姿勢モデル `pose_landmarker_full.bytes` をデスクトップ実行用に `persistentDataPath`（LocalLow）直下へ配置（FileNotFound解消・ローカル環境のみ、リポジトリ外）

---

## RouteFollower「補間注視機能」追加（2025-12-23）

### 目的
移動中に2点間を滑らかに注視する機能を追加（橋の観覧など、視線を連続的に移動させるカメラワーク）

### 問題意識
既存の「1点注視しながら移動」機能では、移動するとずっと同じ方向を向きながら遠ざかる問題があった。
橋を観覧するような場合、橋の根元→中央→終端へと視線を滑らかに移動させたい。

### 追加機能

#### WaypointEventクラス拡張
新規フィールド追加：
- `Transform lookAtStart` - 補間注視の開始地点
- `Transform lookAtEnd` - 補間注視の終了地点

#### RouteFollowerクラス拡張

**状態管理フィールド追加**:
- `Transform interpolationLookAtStart` - 補間注視の開始Transform
- `Transform interpolationLookAtEnd` - 補間注視の終了Transform
- `Vector3 interpolationStartPosition` - 移動開始位置（進捗計算用）
- `Vector3 interpolationEndPosition` - 移動終了位置（進捗計算用）

**メソッド追加**:
- `StartInterpolatedLookAt()` - 補間注視モード開始
- `UpdateInterpolatedLookAt()` - 毎フレーム進捗率計算→目標視線位置補間
- `StopInterpolatedLookAt()` - 補間注視モード終了

**既存メソッド拡張**:
- `AdvanceToNextWaypoint()` - 補間注視モード分岐追加（最優先）
- `RotateTowardsTarget()` - 補間注視優先ロジック追加（最優先→継続的注視→通常回転）
- `CheckContinuousLookAtEnd()` - 補間注視モード終了判定追加

### 動作フロー

**補間注視モード**:
1. WayPointに到達時、lookAtStartとlookAtEndが両方設定されている場合
2. 次のWayPointまでの移動中、移動進捗率（距離ベース）に応じて視線位置を補間
3. 進捗率 = 現在地から開始地点までの距離 / 開始地点から終了地点までの距離
4. 補間位置 = Vector3.Lerp(lookAtStart.position, lookAtEnd.position, 進捗率)
5. 次のWayPoint到達時に自動終了

### Inspector設定例

```yaml
WaypointEvent:
  waypointIndex: 3
  lookAtStart: 橋の根元オブジェクト
  lookAtEnd: 橋の終端オブジェクト
  # WayPoint 3到達時に補間注視開始、WayPoint 4到達時に終了
```

### 優先順位
1. **補間注視モード**（lookAtStart && lookAtEnd）- 最優先
2. **継続的注視モード**（lookWhileMoving）- 既存機能
3. **停止して注視モード**（lookAtTarget && !lookWhileMoving）- 既存機能
4. **通常回転**（Waypoint方向への自動回転）

### 重大なバグ修正
**問題**: 初期実装で`currentWaypointIndex`を使用していたため、到達したばかりのWaypointを終了地点としていた
**修正**: 次のWaypoint（`currentWaypointIndex ± 1`）を終了地点として使用
**対応**: PingPongモード対応（isReversing分岐）、配列外アクセス防止チェック追加

### 動作確認結果
- ✅ コンパイルエラーなし
- ✅ PingPongモード対応完了
- ✅ 既存機能（停止して注視、移動しながら注視）保持
- ⏳ Play Modeテスト（ユーザー側で実施予定）

---

## RouteFollower「移動しながら注視」機能追加（2025-12-16）

### 目的
既存の「停止して注視」機能を残したまま、指定WayPoint間で移動しながら特定地点を注視する機能を追加

### 追加機能

#### WaypointEventクラス拡張
新規フィールド追加：
- `bool lookWhileMoving` - 移動しながら注視するかのフラグ
- `int lookUntilWaypointIndex` - 注視を継続する終了WaypointIndex（-1で無効）

#### RouteFollowerクラス拡張

**状態管理フィールド追加**:
- `Transform continuousLookAtTarget` - 継続的注視の対象
- `int continuousLookAtEndIndex` - 継続的注視の終了WaypointIndex

**メソッド追加**:
- `StartContinuousLookAt()` - 継続的注視モード開始
- `StopContinuousLookAt()` - 継続的注視モード終了
- `CheckContinuousLookAtEnd()` - 終了判定

**既存メソッド拡張**:
- `AdvanceToNextWaypoint()` - lookWhileMoving分岐追加
- `RotateTowardsTarget()` - continuousLookAtTarget優先ロジック追加
- `MoveTowardsCurrentWaypoint()` - 終了判定呼び出し追加

### 動作フロー

**停止して注視モード（既存機能）**:
1. WayPointに到達
2. `lookAtTarget`設定あり、`lookWhileMoving=false`
3. 移動停止 → 注視演出（duration秒間） → カメラ復帰 → 移動再開

**移動しながら注視モード（新機能）**:
1. WayPointに到達
2. `lookAtTarget`設定あり、`lookWhileMoving=true`
3. 移動は継続したまま注視開始
4. `lookUntilWaypointIndex`に到達したら注視解除

### Inspector設定例

```yaml
WaypointEvent:
  waypointIndex: 3
  lookAtTarget: 塔オブジェクト
  lookWhileMoving: true
  lookUntilWaypointIndex: 7
  # WayPoint 3到達時に塔を見始め、WayPoint 7到達時に注視解除
```

### 設計原則
- **既存機能保持**: 停止して注視機能は完全に維持
- **拡張による追加**: 新フラグで動作を切り替え
- **後方互換性**: 既存のWaypointEvent設定は影響なし（lookWhileMoving=false）

### 動作確認結果
- ✅ コンパイルエラーなし
- ✅ アセットリフレッシュ成功
- ⏳ Play Modeテスト（ユーザー側で実施）

---

## ShibuyaStationモデルピボット位置修正（2025-12-16）

### 問題
**症状**: SHibuyaStationオブジェクト（2425個の子オブジェクトを含む）のピボット位置が実際のモデル群の中心から大きくずれていた
- 親GameObject位置: `(-85.6, 0, 0)`
- 子オブジェクト群の実際の中心: 別の位置

**原因**: インポート時のピボット位置設定がモデル群の中心と一致していなかった

### 解決
ユーザー側でUnity Editor上でピボット位置を修正完了

### 結果
- ✅ ピボット位置がモデル群の中心に配置された
- ✅ プレハブ化・シーン配置時の位置調整が容易になった

---

## HerdSim動物アセット統合 & BatMove実装（2025-12-05）

### HerdSim Ratアセット統合

**目的**: トンネル内でネズミがPlayerから逃げる演出を実装

#### 1. RatTestシーン作成
- Plane（Layer 25: Ground、Tag: Ground）配置
- HerdSim Rat Generic prefab配置
- MeshCollider無効化（ネズミの地面検知用）

#### 2. Pipe (2)へのMeshCollider追加
**問題**: ネズミが地面として認識できるよう、Pipeにコライダーが必要
**懸念**: MeshCollider追加で水の挙動が変わる可能性

**解決策**:
- Pipe (2)にMeshCollider追加、Layer 25（Ground）に設定
- Dynamic Waves Simulation ZoneのIntersectionLayerMaskからLayer 25（Ground）とLayer 26（HerdSim）を除外
- 結果：水はGroundレイヤーを無視、ネズミはPipeを地面として認識

#### 3. HerdSimScary.cs改良
**追加機能**: 検知半径をInspectorから調整可能に

**変更内容**:
```csharp
public float _scareRadius = 4.0f;  // 新規追加
Physics.OverlapSphere(transform.position, _scareRadius, _herdLayerMask);  // 4.0f → _scareRadius
```

**効果**: ネズミの逃走開始距離を柔軟に調整可能

#### 4. PlayerCubeからネズミが逃げる設定
- PlayerCubeにHerdSimScaryコンポーネント追加
- _scareType: [4]（ネズミのtype）
- _herdLayerMask: Layer 26（HerdSim）
- _scareRadius: Inspector調整可能

**動作**: PlayerCubeが設定半径内にネズミを検知すると、ネズミが逃走

#### HerdSimのLayerMask構造理解
**_groundLayerMask** (33554432 = Layer 25のみ):
- 地面検知用（下方向Raycast）

**_pushyLayerMask** (100663297 = Layer 0, 25, 26):
- 障害物回避用（前方・左前・右前Raycast）
- Layer 0（Default）、Layer 25（Ground）、Layer 26（HerdSim）を含む

**結果**: 壁をLayer 25に設定すれば自動的に回避

---

### BatMove.cs実装（蝙蝠飛行制御）

**目的**: RouteFollowerのWayPointEventから蝙蝠を飛ばす演出を実装

**配置場所**: `Assets/Scripts/BatMove.cs`

#### 仕様
**SerializeFieldパラメータ**:
- `animator` - Animatorコンポーネント（未設定時は自動取得）
- `moveSpeed` - 移動速度（デフォルト: 5.0）
- `targetPosition` - 移動先Transform

**公開関数**:
- `StartFly()` - EventListenerから呼び出し

**動作フロー**:
1. Animator.SetTrigger("ReachEvent") - 飛行アニメーション開始
2. targetPositionまで滑らかに移動（Vector3.MoveTowards使用）
3. 到着後、gameObject.SetActive(false)

#### WayPointEvent統合
**onReached発火タイミング**:
- LookAt演出あり → 注視演出完了後
- LookAt演出なし → ウェイポイント到達直後
- **常にシーン遷移より前に実行**

**使用方法**:
1. BatにBatMoveコンポーネント追加
2. RouteFollowerのWayPointEvent.onReachedに`BatMove.StartFly()`を設定
3. Inspector設定: moveSpeed、targetPosition

---

## MediaPipeビルド対応（StreamingAssets mode切り替え）（2025-12-03）

### 問題
**症状**: プロジェクトをビルドすると、カメラ映像（RawImage）が真っ白で、腕振り検知が動作しない
- Editor環境では正常動作
- ビルド環境ではMediaPipeが初期化されない

### 根本原因
**AppSettings.assetがEditor専用モード（Local mode）に設定されていた**

**詳細**:
- `AppSettings.asset` 17行目: `_assetLoaderType: 2`（Local mode）
- LocalResourceManagerは`AssetDatabase` API（Editor専用）に依存
- Packagesフォルダはビルドに含まれない
- Bootstrap.cs:56-66で`#if UNITY_EDITOR`ディレクティブによりビルド環境では即座にエラー

**設定者**: MediaPipeUnityPluginの作者がサンプルシーン用にEditor環境テスト用途で設定
**プロジェクト開始時**: サンプルシーンのデフォルト設定をそのまま流用

### 解決策実施内容

#### 1. StreamingAssetsフォルダ作成
```bash
mkdir "Assets/StreamingAssets/MediaPipe"
```

#### 2. モデルファイルをコピー
Packagesから必要なPose Landmarkerモデルファイルをコピー：
```bash
cp "Packages/.../pose_landmarker_lite.bytes" "Assets/StreamingAssets/MediaPipe/"
cp "Packages/.../pose_landmarker_full.bytes" "Assets/StreamingAssets/MediaPipe/"
cp "Packages/.../pose_landmarker_heavy.bytes" "Assets/StreamingAssets/MediaPipe/"
```

**コピーしたファイル**:
- pose_landmarker_lite.bytes (5.5MB)
- pose_landmarker_full.bytes (9.0MB)
- pose_landmarker_heavy.bytes (29.3MB)

#### 3. AppSettings.asset修正
`Assets/MediaPipeUnity/Samples/Scenes/AppSettings.asset` 17行目を変更：
```yaml
変更前: _assetLoaderType: 2  # Local (Editor専用)
変更後: _assetLoaderType: 0  # StreamingAssets (ビルド対応)
```

### 技術的背景

**Local modeの制約**:
- `AssetDatabase.LoadAssetAtPath<TextAsset>()` - Editor専用API使用
- `Packages/`フォルダ依存 - ビルドに含まれない

**StreamingAssets modeの特徴**:
- 標準的なFile API使用（ビルド環境で動作）
- `Application.streamingAssetsPath`使用（ビルドに含まれる）
- Android/WebGLでも動作（UnityWebRequest対応）

**メモリリーク・不具合リスク**: なし
- 両方とも同一インターフェース（`IResourceManager`）実装
- 最終的な処理は同じ（`Application.persistentDataPath`にコピー）
- StreamingAssets modeはUnityの推奨手法

### 動作確認結果
- ✅ StreamingAssetsフォルダ作成成功
- ✅ モデルファイルコピー成功（3ファイル、合計45.8MB）
- ✅ AppSettings.asset変更成功（_assetLoaderType: 0）
- ✅ アセットリフレッシュ成功
- ✅ コンパイルエラーなし
- ✅ **Editor環境テスト完璧**（ユーザー確認済み）
- ✅ **ビルド環境テスト完璧**（ユーザー確認済み）
  - カメラ映像正常表示
  - 腕振り検知正常動作

### 結論
StreamingAssets mode切り替えにより、ビルド環境でMediaPipeが正常に動作することを確認。
Editor専用設定からビルド対応設定への移行が完了。

---

## カメラ映像表示/非表示切り替え機能実装（2025-12-03）

### 目的
Shiftキー入力でAnnotatable Screen（右下のカメラ映像UI）の表示/非表示を切り替える機能を実装

### 実装内容

#### CameraViewToggle.cs作成
**場所**: `Assets/Scripts/CameraViewToggle.cs`

**責任**: カメラUI表示制御のみに専念（単一責任原則）

**機能**:
- Shiftキー（左右どちらでも可）で表示/非表示をトグル
- 同じGameObject内のRawImageコンポーネントを自動取得（`GetComponent<RawImage>()`）
- Inspector設定不要

**実装詳細**:
```csharp
public class CameraViewToggle : MonoBehaviour
{
    private RawImage cameraRawImage;

    private void Awake()
    {
        cameraRawImage = GetComponent<RawImage>();
    }

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.LeftShift) || Input.GetKeyDown(KeyCode.RightShift))
        {
            ToggleCameraView();
        }
    }

    private void ToggleCameraView()
    {
        if (cameraRawImage != null)
        {
            cameraRawImage.enabled = !cameraRawImage.enabled;
        }
    }
}
```

### 設定方法

#### 各シーンへの適用
1. **現在ロード中のシーン**: Annotatable ScreenにCameraViewToggleコンポーネントをアタッチ済み
2. **残り5シーン**: ユーザー側でPrefab更新またはシーンごとに手動アタッチ

#### Prefab更新方法（推奨）
1. Hierarchy上の「Annotatable Screen」を選択
2. Inspector上部の「Overrides」ボタンをクリック
3. 「Apply All」を選択 → MediaPipeCanvas.prefabに反映
4. 全シーンに自動適用される

### 動作確認
- Play Modeで**Shiftキー**を押すとカメラ映像が表示/非表示切り替え
- RouteFollowerのスペースキー実装と同じパターン

### 設計原則
- **単一責任原則**: CameraViewToggleはカメラ表示制御のみ担当
- **参照自動取得**: GetComponentで同じGameObject内のRawImageを自動取得
- **外部ライブラリ非依存**: MediaPipeのコードを直接編集せず、独立したコンポーネントとして実装

---

## シーン遷移後カメラ映像停止問題解決（2025-11-30）

### 問題
**症状**: シーン遷移後、Annotatable Screenのカメラ映像が静止画になる

**初回シーン**: カメラ映像が正常に動作
**シーン遷移後**: 静止画のまま更新されない

### 調査過程

#### 試行1: Stop()オーバーライド（失敗）
- **仮説**: PoseLandmarkerRunner.Stop()がSharedCameraImageSourceを停止
- **修正**: Stop()をオーバーライドしてImageSource.Stop()をスキップ
- **結果**: ❌ 効果なし

#### 試行2: SharedCameraImageSource再作成（失敗）
- **仮説**: シーン遷移後にImageSourceが正しく設定されていない
- **修正**: PoseLandmarkerRunner.Run()で毎回SharedCameraImageSourceを新規作成
- **結果**: ❌ 効果なし（ImageSource参照は正しかった）

#### 試行3: ログ追加で真相発見（成功）
- **調査**: CameraInput.Update()にログを追加
- **発見**: シーン遷移後に`webCamTexture.isPlaying=False`になっていた

**ログ比較**:
```
初回シーン:
[CameraInput] Update() - Frame updated. Count: 1200

シーン遷移後:
[CameraInput] Update() - Not updating: isInitialized=True, webCamTexture.isPlaying=False
```

### 根本原因
**WebCamTextureの停止**: シーン遷移時にWebCamTextureが停止し、CameraInput.Update()でのフレーム更新が止まっていた

**影響範囲**:
1. WebCamTextureが停止 → `webCamTexture.didUpdateThisFrame=false`
2. CameraInput.Update()で`currentFrame.SetPixels()`が実行されない
3. currentFrame（Texture2D）が最後のフレームのまま固定
4. Annotatable Screenに静止画が表示される

### 解決策

#### CameraInput.cs修正
```csharp
public void EnsureCameraIsPlaying()
{
    if (webCamTexture != null && !webCamTexture.isPlaying)
    {
        Debug.Log("[CameraInput] WebCamTexture was stopped, restarting...");
        webCamTexture.Play();
    }
}
```

#### PoseLandmarkerRunner.cs修正
```csharp
// シーン遷移後にWebCamTextureが停止している場合は再起動
cameraInput.EnsureCameraIsPlaying();

var sharedSource = new SharedCameraImageSource(cameraInput);
yield return sharedSource.Play();
```

### 動作確認結果
- ✅ シーン遷移後もカメラ映像が正常に動作
- ✅ WebCamTextureが自動的に再起動される
- ✅ Annotatable Screenのカメラ映像が更新され続ける

### 学び
- **DontDestroyOnLoad使用時の注意**: シーン遷移時にコンポーネントの状態が変化する可能性がある
- **WebCamTextureのライフサイクル**: シーン遷移時に自動的に停止することがある
- **デバッグログの重要性**: Update()に状態ログを追加することで真相が判明
- **仮説検証の重要性**: 複数の仮説を立てて順番に検証する

---

## 水流制御システム & FadeManager GameEvent統合（2025-11-28）

### WaterFlowController.cs作成
**目的**: EventListenerから水の流量（ConstantFlowRate）を制御

**機能**:
- `SetFlowRateHigh()` - フェード中の高速流量（1.0）
- `SetFlowRateLow()` - 通常時の低速流量（0.008）
- `SetFlowRateCustom(float)` - 任意の流量値に設定

**対象**:
- GameObject: `Dynamic Waves Object`
- Component: `KWS_DynamicWavesObject`
- Field: `ConstantFlowRate`

**使用方法**:
1. `Dynamic Waves Object`に`WaterFlowController.cs`をアタッチ
2. Inspector で `targetWaterObject` に自身を参照設定
3. EventListenerから`SetFlowRateLow()`等を呼び出し

---

### FadeManager.cs GameEvent統合
**目的**: FadeManagerの単一責任原則を保ちつつ、Fade待機完了時に任意のイベントを発火可能にする

**設計原則**:
- ✅ FadeManagerはFade演出のみに専念（Fade以外の処理を持たない）
- ✅ GameEvent（ScriptableObject）を使って疎結合
- ✅ 拡張性の確保（今後のFade時イベント需要に対応）

**追加フィールド**:
- `GameEvent onFadeInDelayComplete` - FadeIn待機完了時に発火
- `GameEvent onFadeOutDelayComplete` - FadeOut待機完了時に発火

**発火タイミング**:
- FadeIn(): delay待機完了 → **GameEvent発火** → 実際のフェード演出開始
- FadeOut(): delay待機完了 → **GameEvent発火** → 実際のフェード演出開始

**実装例（水流制御）**:
1. GameEventアセット作成（例：`OnFadeInDelayComplete.asset`）
2. FadeManagerのInspectorで`onFadeInDelayComplete`に設定
3. GameEventListenerを`Dynamic Waves Object`に配置
4. GameEventListenerのresponseに`WaterFlowController.SetFlowRateLow()`を設定

**結果**: FadeIn待機完了時に自動的に水流が低速（0.008）に切り替わる

---

## SetFieldValue改良 & ホラーゲーム風ライティング設定（2025-11-27）

### InspectorFieldSetterMCPTool.cs改良
**目的**: Volume ProfileのようなネストされたScriptableObjectフィールドに対応

#### ResolveNestedFieldPath()メソッド追加
**機能**:
- ドット記法対応（例：`profile.intensity.value`）
- 配列/リストインデックスアクセス対応（例：`components[0].fixedExposure`）
- 実際のランタイム型を使用（基底クラスではなく派生クラスの型）

**改良箇所**:
```csharp
// 配列/リスト要素取得時に実際のランタイム型を使用
nextType = nextObject != null ? nextObject.GetType() : nextType.GetElementType();
```

### ホラーゲーム風ライティング設定実装（完了）

#### 設定内容
**シーン**: RiverHoleTonnnelMap
**対象**: Main Camera > Volume > Main Camera Profile

#### **最終設定値**（全てのVolume Overrideを使用）:

**ColorAdjustments** (components[0]):
- `postExposure`: **-1.5**（暗闇演出、ポイントライト視認可能）
- `contrast`: **60**（高コントラスト）
- `saturation`: **-80**（モノクロ寄り）

**Vignette** (components[1]):
- `intensity`: **0.5**
- `smoothness`: **0.35**
- 画面周辺を暗くしてホラー感強調

**FilmGrain** (components[2]):
- `intensity`: **0.4**
- ホラー映画風の粒子感

**ChromaticAberration** (components[3]):
- `intensity`: **0.3**
- 色収差効果で不気味な雰囲気

#### 元の設定（バックアップ）
- `postExposure`: **-0.95**
- `contrast`: **28**
- `saturation`: **-53**

### 設定を元に戻す手順
```bash
# 元の暗いトンネル風設定に戻す
curl -X POST http://localhost:56780/mcp/ -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"SetFieldValue","arguments":{"objectName":"Main Camera","componentTypeName":"Volume","fieldName":"sharedProfile.components[0].postExposure.m_Value","value":"-0.95"}},"id":1}'

curl -X POST http://localhost:56780/mcp/ -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"SetFieldValue","arguments":{"objectName":"Main Camera","componentTypeName":"Volume","fieldName":"sharedProfile.components[0].contrast.m_Value","value":"28"}},"id":1}'

curl -X POST http://localhost:56780/mcp/ -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"SetFieldValue","arguments":{"objectName":"Main Camera","componentTypeName":"Volume","fieldName":"sharedProfile.components[0].saturation.m_Value","value":"-53"}},"id":1}'
```

### 動作確認結果
- ✅ SetFieldValueでVolume Profileのネストされたフィールドにアクセス可能
- ✅ 全Volume Override追加・設定完了（ColorAdjustments、Vignette、FilmGrain、ChromaticAberration）
- ✅ ホラーゲーム風設定をシーンに反映成功
- ✅ Play Modeテスト成功：ポイントライトはしっかり明るく、それ以外は暗闇
- ✅ リッチな質感達成（粒子感、色収差、周辺減光の複合効果）
- ✅ ユーザー確認済み：「とってもいい感じです　すばらしい」

---

## シーン遷移システム仕様変更 & 自動前進機能追加（2025-11-24）

### 仕様変更: 円環状一方通行シーン接続
**変更理由**: シーン接続が円環状一方通行のため、ポータルID・スポーン地点ID不要

#### ScenePortal.cs修正
- SpawnPoint[]配列削除 → **startPosition（固定1箇所）**
- Enter(string, string) → **Enter(string)**（targetSceneNameのみ）
- s_nextPortalID削除 → **s_shouldSpawnフラグ管理**
- CheckAndSpawnPlayer()シンプル化（ID判定なし）

#### RouteFollower.cs修正
- WaypointEvent拡張:
  - UnityEvent<string, string> → **UnityEvent<string>**
  - enableSceneTransition, targetSceneName追加
  - onSceneTransition追加（ScenePortal.Enter用）
- destinationPortalID削除

### 自動前進機能実装（テスト用）
**目的**: 腕振り不要でシーン遷移テスト可能に

#### RouteFollower.cs機能追加
- **autoMoveEnabled**フラグ追加（Inspector: Debug/Testing）
- Update()でautoMoveEnabled時に腕振り検知無視して強制前進

#### InspectorFieldSetterMCPTool.cs改良
- SetFieldValue()のprivate [SerializeField]対応（BindingFlags.NonPublic追加）

### 問題修正
1. **_FadeManager表示問題**:
   - RectTransform削除 → 空のGameObject再作成
   - Canvas/CanvasScaler/GraphicRaycaster削除不要（FadeManager.csが自動初期化）

2. **RouteFollowerモード問題**:
   - PingPong → **OneWay**に修正

3. **UnityEvent型不一致問題**:
   - UnityEvent（0パラメータ）→ UnityEvent<string>（1パラメータ）に修正
   - ScenePortal.Enter(string, string) → Enter(string)に修正

### 動作確認結果
- ✅ シーン遷移成功（SceneMoveTest → SceneMoveTest）
- ✅ スポーン位置配置成功（SpawnPoint_Start）
- ✅ 自動前進モード動作確認
- ⚠️ カメラ起動エラー（既存問題、自動前進モードでは無視可能）

---

## FadeManagerシステム完全作り直し（2025-11-24）

### 旧システムの問題点
1. **DontDestroyOnLoad使用**: シーン遷移後にCanvas/Imageが破壊される不具合
2. **Singleton設計**: 各シーンに配置する設計と相性が悪い
3. **タイミング制御の複雑さ**: 呼び出し側でFadeOut→SceneLoad→FadeInを管理
4. **小さな黒いキャンバス問題**: シーン遷移後に子のImageが消失

### 新システムの設計
1. **各シーンに配置**: DontDestroyOnLoad完全削除
2. **自動フェードイン**: シーン開始時に`SetFadeBlack()` → `FadeIn()`自動実行
3. **LoadSceneWithFade(string)**: FadeOut完了まで待機してからシーンロード
4. **IsFading静的プロパティ**: フェード中のプレイヤー停止（RouteFollower.Update()で判定）

### 実装内容
**FadeManager.cs**:
- `public static bool IsFading { get; private set; }` 追加
- `LoadSceneWithFade(string sceneName, float fadeOutDuration)` 実装
- `Awake()`: fadeImage未設定時に自動生成（Canvas+Image全画面黒パネル）
- `Start()`: autoFadeInOnStart=trueでシーン開始時に自動フェードイン
- Singleton削除、DontDestroyOnLoad削除

**ScenePortal.cs**:
- スポーン機能完全削除（startPosition, player, s_shouldSpawn, CheckAndSpawnPlayer()）
- `Enter(string targetSceneName)` - シーン遷移トリガーのみ
- FadeManagerに委譲

**RouteFollower.cs**:
- `Update()`冒頭で`if (FadeManager.IsFading) return;`追加
- フェード中はプレイヤーの移動・回転を完全停止

### セットアップ手順
**各シーンに配置**:
1. FadeCanvas作成（Canvas: ScreenSpaceOverlay, sortingOrder=9999）
2. FadeImage作成（子要素、黒色、RectTransform全画面）
3. FadeCanvasにFadeManagerコンポーネントアタッチ
4. FadeManager.fadeImageにFadeImageを参照設定

### 動作確認結果
- ✅ シーン遷移時に正しくフェードアウト→ロード実行
- ✅ 次シーンで自動的にフェードイン
- ✅ フェード中にプレイヤーの動き停止
- ✅ Canvas/Image破壊問題解決

### Unity.md更新
**🗑️ 不要コード・ゴミファイル削除の絶対原則**追加:
- 旧システム作り直し時は、コード+GameObject+Inspector参照を一括削除
- Grep検索で旧GameObject名を確認
- 完全にクリーンな状態にしてから「完了」と報告
- 放置は絶対禁止

---

## シーン遷移システム最適化（2025-11-26）

### 第1段階：ScenePortal純粋C#クラス化
**変更理由**: MonoBehaviourの継承が不要、動的インスタンス化でシンプル化

#### ScenePortal.cs
- MonoBehaviour継承削除 → **純粋なC#クラス**
- 無意味なコルーチン削除

#### RouteFollower.cs
- `UnityEvent<string> onSceneTransition`削除
- ScenePortalを`new`でインスタンス化して使用

### 第2段階：責任分離の適正化（同日追加）
**問題**: FadeManagerがシーン遷移処理まで担当していた（責任過多）

#### FadeManager.cs
- `LoadSceneWithFade()`削除
- **`FadeOutWithCallback()`追加** - フェード演出のみ担当
- シーン遷移の責任を完全に除去

#### ScenePortal.cs
- **シーン遷移の責任を担当**
- FadeManagerのコールバック方式を利用
```csharp
fadeManager.FadeOutWithCallback(() =>
{
    SceneManager.LoadScene(targetSceneName);
});
```

### 責任分離の結果
- **FadeManager**: フェード演出**のみ**
- **ScenePortal**: シーン遷移**のみ**（純粋C#クラス）
- 各クラスが単一責任原則を遵守

### 第3段階：演出完了待機問題の修正（同日追加）
**問題**: LookAt演出を待たずにシーン遷移が発生していた

#### RouteFollower.cs
- **AdvanceToNextWaypoint()修正** - 全ての処理を順序立てて実行
- **LookAtWithPause()改良** - コールバック引数追加
```csharp
// LookAt演出完了後に他の処理を実行
System.Action executeAfterEffects = () => {
    evt.onReached?.Invoke();  // 追加イベント
    // シーン遷移（最後に実行）
    if (evt.enableSceneTransition) {
        ScenePortal portal = new ScenePortal();
        portal.Enter(evt.targetSceneName);
    }
};
```

### 処理順序の保証
1. LookAt演出（存在する場合）
2. onReachedイベント
3. シーン遷移（最後）

コールバック方式により、演出完了を待ってから次の処理を実行

---

## 次回作業予定

### 拡張作業
1. Build Settingsにシーン追加
2. 複数シーン作成（Map1 ⇔ Map2 円環接続）
3. 各シーンの固定開始位置設定
4. 円環状一方通行遷移テスト

---

## プロジェクト構成情報

### カスタムMCPツール配置場所
**配置ディレクトリ**: `Assets/Scripts/MCPTools/`

**確認コマンド**:
```bash
cd "C:\Users\Simna\KWS2 DynamicWaterAsset" && find Assets/Scripts/MCPTools -name "*MCPTool*.cs" 2>/dev/null
```

**登録済みカスタムMCPツール数**: 27個

**主要ツール**:
- AllMCPToolsBuilder.cs - 全ツール登録管理
- InspectorFieldSetterMCPTool.cs - SetFieldValue（Volume Profile操作等）
- RefreshMCPToolsMCPTool.cs - 完全自動化ツール
- ComponentAttachMCPTool.cs, GameObjectManagerMCPTool.cs等

---

## 参考リソース

- YOLOv8 ONNX Export: https://docs.ultralytics.com/modes/export/
- ONNX Runtime: https://github.com/microsoft/onnxruntime
- KWS Water System: https://kripto289.gitbook.io/kripto289-docs
- MediaPipeUnityPlugin: https://github.com/homuler/MediaPipeUnityPlugin
- Unity WebCamTexture: https://docs.unity3d.com/ScriptReference/WebCamTexture.html

---

## 開発履歴（要約）

- **2025年10月18日**: 人物検知システム移植完了
- **2025年10月21日**: イベントシステム実装完了
- **2025年11月1日**: 腕振り歩行検知システム実装完了、MediaPipe統合完了
- **2025年11月4-5日**: ルート移動システム実装完了、カメラ注視演出実装完了、カメラ統合システム実装完了
- **2025年11月21日**: シーン遷移システム実装完了（FadeManager + ScenePortal）
- **2025年11月24日**: FadeManagerシステム完全作り直し、不要コード削除ルール追加（Unity.md）
- **2025年11月27日**: InspectorFieldSetterMCPTool.cs改良（ネストされたScriptableObject対応）、ホラーゲーム風ライティング設定完了（全Volume Override使用）