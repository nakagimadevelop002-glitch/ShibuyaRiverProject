# 開発記録

## プロジェクト概要
Python依存の人物検知システムをC#のみに移植し、Unity内で完結するシステムを構築。
腕振り検知でプレイヤーが川沿いをルート移動し、複数マップ間を遷移可能。

---

## 実装済みシステム

### 1. 人物検知システム
- **CameraInput.cs**: WebCamTextureでカメラ入力取得
- **PersonDetector.cs**: YOLOv8 ONNX Runtime推論（3フレームごと）
- **PeopleWaterController.cs**: 人数→水面透明度・濁り色変化

### 2. 腕振り歩行検知システム
- **MediaPipeUnityPlugin**: Pose Landmarker（33関節検出）
- **PoseLandmarkProvider.cs**: MediaPipeデータ提供（Singleton）
- **ArmSwingDetector.cs**: 腕振り動作検知（時系列解析）
- **PlayerWalkController.cs**: Player前進制御

### 3. ルート移動システム
- **RouteFollower.cs**: リングフィットアドベンチャー方式
  - ウェイポイント配列でルート設定
  - 腕振り検知で前進/停止
  - OneWay/Loop/PingPongモード対応
  - 3次元移動対応（高低差対応）
- **CameraViewController.cs**: カメラ注視演出

### 4. イベントシステム
- **GameEvent.cs**: ScriptableObjectベースイベント
- **GameEventListener.cs**: イベント購読・UnityEvent実行
- 用途: 演出システムの疎結合化

### 5. シーン遷移システム（✅最新 2025-11-24 完全作り直し）
- **FadeManager.cs**: Fade演出管理コンポーネント
  - 責任: 画面フェード演出、シーン遷移制御
  - **設計方針**: 各シーンに配置、DontDestroyOnLoad不使用
  - **自動フェードイン**: シーン開始時に自動的に暗転→明転
  - **LoadSceneWithFade(string sceneName)**: FadeOut完了後にシーンロード
  - **IsFading静的プロパティ**: フェード中のプレイヤー停止制御
  - **フォールバック**: fadeImage未設定時は自動生成（Canvas+Image）
  - 汎用性: シーン遷移、カットシーン、演出等で使用可能
- **ScenePortal.cs**: シーン遷移トリガー
  - 責任: シーン遷移のトリガー実行のみ
  - `Enter(string targetSceneName)` - WaypointEventから呼ばれる
  - FadeManagerに委譲してシーン遷移実行
  - **スポーン機能削除**: 各シーンに固定配置されたPlayerがそのまま使用される

**設計哲学**:
- **単一責任原則**: ScenePortal=遷移トリガー、FadeManager=Fade演出+シーン遷移
- **Tell, Don't Ask**: ScenePortalがFadeManagerに「このシーンに遷移して」と命令
- **シンプル化**: DontDestroyOnLoad不使用、各シーンに配置で完結
- **自己完結**: 各シーンのFadeManagerが自動的にフェードイン実行

---

## 実装完了ファイル一覧

```
Assets/
├── yolov8n.onnx (13MB) ...................... YOLOv8モデル
├── Plugins/OnnxRuntime/ ..................... ONNX Runtime DLL
├── Editor/
│   └── SceneViewSkyboxFix.cs ................ Skybox自動有効化
└── Scripts/
    ├── CameraInput.cs ....................... カメラ入力
    ├── PersonDetector.cs .................... 人物検知
    ├── PeopleWaterController.cs ............. 水面連動
    ├── PoseLandmarkProvider.cs .............. MediaPipeデータ提供
    ├── ArmSwingDetector.cs .................. 腕振り検知
    ├── PlayerWalkController.cs .............. Player前進制御
    ├── RouteFollower.cs ..................... ルート移動管理
    ├── CameraViewController.cs .............. カメラ注視演出
    ├── GameEvent.cs ......................... イベントシステム
    ├── GameEventListener.cs ................. イベントリスナー
    ├── FadeManager.cs ....................... Fade演出管理（NEW）
    ├── ScenePortal.cs ....................... シーン遷移管理（NEW）
    └── [その他既存システム]
```

---

## シーン遷移システム詳細

### 使用方法
```
RouteFollower > Waypoint Events > Element 0:
├─ waypointIndex: 10
└─ onReached（UnityEvent）:
    └─ ScenePortal.Enter("Map2", "FromMap1_ExitA")
```

### 双方向移動設定例
```
Map1シーン:
└─ ScenePortal
    └─ Spawn Points:
        ├─ Portal ID: "FromMap2_A" → [Map1_EntryA Transform]
        └─ Portal ID: "FromMap3_B" → [Map1_EntryB Transform]

Map2シーン:
└─ ScenePortal
    └─ Spawn Points:
        └─ Portal ID: "FromMap1_ExitA" → [Map2_Entry Transform]
```

### セットアップ手順（次回作業）
1. **FadeManager配置**:
   - 最初のシーンに空のGameObject作成
   - FadeManagerコンポーネントをアタッチ

2. **各シーンにScenePortal配置**:
   - 空のGameObject作成（位置は原点でOK）
   - ScenePortalコンポーネントをアタッチ
   - SpawnPoints配列設定:
     - Portal ID: 文字列識別子
     - Position: スポーン位置Transform

3. **Player設定**:
   - PlayerオブジェクトにTag="Player"設定

4. **WaypointEvent設定**:
   - RouteFollowerのInspector
   - Waypoint Events > onReached
   - ScenePortal.Enter(シーン名, ポータルID)を登録

5. **動作テスト**:
   - Waypoint到達 → シーン遷移確認
   - スポーン位置確認
   - Fade演出確認

---

## 技術仕様

### パフォーマンス
- **YOLO推論**: 50-150ms/フレーム、3フレームごと
- **MediaPipe**: 33関節リアルタイム追跡
- **カメラ**: 640x640, 30fps

### 主要アーキテクチャ
```
カメラ → YOLO（人数検知） → 水面変化
      → MediaPipe（腕振り検知） → Player前進
                                 ↓
                            RouteFollower
                                 ↓
                         Waypoint到達時
                                 ↓
                    ScenePortal.Enter() 呼び出し
                                 ↓
                         FadeManager.FadeOut()
                                 ↓
                         シーン読み込み
                                 ↓
                         FadeManager.FadeIn()
                                 ↓
                      ScenePortal.CheckAndSpawnPlayer()
```

---

## MCPツール拡張履歴

### InspectorFieldSetterMCPTool拡張（2025-11-23）
**目的**: カスタムクラス配列・UnityEvent設定の完全自動化

**追加メソッド**:
1. **SetArraySize** - 配列サイズ設定
   - カスタムクラス要素の自動初期化対応（`Activator.CreateInstance`）
   - プリミティブ・Unity Object対応
   - シーン自動保存

2. **SetArrayElementField** - 配列要素フィールド設定
   - GameObject/Transform/Component参照設定
   - プリミティブ型設定（string, int, float, bool等）
   - Value type（struct）の書き戻し対応
   - シーン自動保存

3. **AddUnityEventListener** - UnityEventリスナー追加
   - SerializedObject/SerializedProperty API使用
   - 0〜2パラメータメソッド対応
   - パラメータ型自動検出（String/Int/Float/Bool/Object）
   - ネストフィールドパス対応（例: `waypointEvents.Array.data[0].onReached`）
   - シーン自動保存

**設計原則**: 汎用性最優先。特定クラス専用ツール作成を完全排除。

---

## シーン遷移テスト環境構築完了（2025-11-23）

### 実施内容
**SceneMoveTestシーンにシーン遷移テスト環境を完全構築**

1. **SpawnPoint設定**:
   - SpawnPoint_Start GameObject作成（位置: 0,0,0）
   - ScenePortal.spawnPoints[0]設定完了
     - portalID: "Start"
     - position: SpawnPoint_Start Transform

2. **WaypointEvent設定**:
   - RouteFollower.waypointEvents[0]設定完了
     - waypointIndex: 1（Waypoint2到達時）
     - lookDuration: 3.0秒
     - onReached: ScenePortal.Enter("SceneMoveTest", "Start")

3. **RouteFollower参照設定**（前回作業）:
   - armSwingDetector: ArmSwingDetector
   - playerCamera: Main Camera
   - waypoints: [Waypoint1, Waypoint2]

### テスト手順
1. Play Mode開始
2. 腕振り検知でRiverWalkerがWaypoint2に到達
3. waypointEvents[0].onReached発火 → ScenePortal.Enter()呼び出し
4. Fade演出 → シーン再読み込み → Playerがスポーン位置に移動

### 現在の状態
- ✅ コード実装: 100%完了
- ✅ シーン設定: 100%完了（テスト環境）
- ✅ 動作テスト: 完了（シーン遷移成功確認）

---

## シーン遷移システム仕様変更 & 自動前進機能追加（2025-11-24）

### 仕様変更: 円環状一方通行シーン接続
**変更理由**: シーン接続が円環状一方通行のため、ポータルID・スポーン地点ID不要

#### ScenePortal.cs修正
- SpawnPoint[]配列削除 → **startPosition（固定1箇所）**
- Enter(string, string) → **Enter(string)**（targetSceneNameのみ）
- s_nextPortalID削除 → **s_shouldSpawnフラグ管理**
- CheckAndSpawnPlayer()シンプル化（ID判定なし）

#### RouteFollower.cs修正
- WaypointEvent拡張:
  - UnityEvent<string, string> → **UnityEvent<string>**
  - enableSceneTransition, targetSceneName追加
  - onSceneTransition追加（ScenePortal.Enter用）
- destinationPortalID削除

### 自動前進機能実装（テスト用）
**目的**: 腕振り不要でシーン遷移テスト可能に

#### RouteFollower.cs機能追加
- **autoMoveEnabled**フラグ追加（Inspector: Debug/Testing）
- Update()でautoMoveEnabled時に腕振り検知無視して強制前進

#### InspectorFieldSetterMCPTool.cs改良
- SetFieldValue()のprivate [SerializeField]対応（BindingFlags.NonPublic追加）

### 問題修正
1. **_FadeManager表示問題**:
   - RectTransform削除 → 空のGameObject再作成
   - Canvas/CanvasScaler/GraphicRaycaster削除不要（FadeManager.csが自動初期化）

2. **RouteFollowerモード問題**:
   - PingPong → **OneWay**に修正

3. **UnityEvent型不一致問題**:
   - UnityEvent（0パラメータ）→ UnityEvent<string>（1パラメータ）に修正
   - ScenePortal.Enter(string, string) → Enter(string)に修正

### 動作確認結果
- ✅ シーン遷移成功（SceneMoveTest → SceneMoveTest）
- ✅ スポーン位置配置成功（SpawnPoint_Start）
- ✅ 自動前進モード動作確認
- ⚠️ カメラ起動エラー（既存問題、自動前進モードでは無視可能）

---

## FadeManagerシステム完全作り直し（2025-11-24）

### 旧システムの問題点
1. **DontDestroyOnLoad使用**: シーン遷移後にCanvas/Imageが破壊される不具合
2. **Singleton設計**: 各シーンに配置する設計と相性が悪い
3. **タイミング制御の複雑さ**: 呼び出し側でFadeOut→SceneLoad→FadeInを管理
4. **小さな黒いキャンバス問題**: シーン遷移後に子のImageが消失

### 新システムの設計
1. **各シーンに配置**: DontDestroyOnLoad完全削除
2. **自動フェードイン**: シーン開始時に`SetFadeBlack()` → `FadeIn()`自動実行
3. **LoadSceneWithFade(string)**: FadeOut完了まで待機してからシーンロード
4. **IsFading静的プロパティ**: フェード中のプレイヤー停止（RouteFollower.Update()で判定）

### 実装内容
**FadeManager.cs**:
- `public static bool IsFading { get; private set; }` 追加
- `LoadSceneWithFade(string sceneName, float fadeOutDuration)` 実装
- `Awake()`: fadeImage未設定時に自動生成（Canvas+Image全画面黒パネル）
- `Start()`: autoFadeInOnStart=trueでシーン開始時に自動フェードイン
- Singleton削除、DontDestroyOnLoad削除

**ScenePortal.cs**:
- スポーン機能完全削除（startPosition, player, s_shouldSpawn, CheckAndSpawnPlayer()）
- `Enter(string targetSceneName)` - シーン遷移トリガーのみ
- FadeManagerに委譲

**RouteFollower.cs**:
- `Update()`冒頭で`if (FadeManager.IsFading) return;`追加
- フェード中はプレイヤーの移動・回転を完全停止

### セットアップ手順
**各シーンに配置**:
1. FadeCanvas作成（Canvas: ScreenSpaceOverlay, sortingOrder=9999）
2. FadeImage作成（子要素、黒色、RectTransform全画面）
3. FadeCanvasにFadeManagerコンポーネントアタッチ
4. FadeManager.fadeImageにFadeImageを参照設定

### 動作確認結果
- ✅ シーン遷移時に正しくフェードアウト→ロード実行
- ✅ 次シーンで自動的にフェードイン
- ✅ フェード中にプレイヤーの動き停止
- ✅ Canvas/Image破壊問題解決

### Unity.md更新
**🗑️ 不要コード・ゴミファイル削除の絶対原則**追加:
- 旧システム作り直し時は、コード+GameObject+Inspector参照を一括削除
- Grep検索で旧GameObject名を確認
- 完全にクリーンな状態にしてから「完了」と報告
- 放置は絶対禁止

---

## 次回作業予定

### 拡張作業
1. 複数シーン作成（Map1 ⇔ Map2 円環接続）
2. 各シーンの固定開始位置設定
3. 円環状一方通行遷移テスト

---

## 参考リソース

- YOLOv8 ONNX Export: https://docs.ultralytics.com/modes/export/
- ONNX Runtime: https://github.com/microsoft/onnxruntime
- KWS Water System: https://kripto289.gitbook.io/kripto289-docs
- MediaPipeUnityPlugin: https://github.com/homuler/MediaPipeUnityPlugin
- Unity WebCamTexture: https://docs.unity3d.com/ScriptReference/WebCamTexture.html

---

## 開発履歴（要約）

- **2025年10月18日**: 人物検知システム移植完了
- **2025年10月21日**: イベントシステム実装完了
- **2025年11月1日**: 腕振り歩行検知システム実装完了、MediaPipe統合完了
- **2025年11月4-5日**: ルート移動システム実装完了、カメラ注視演出実装完了、カメラ統合システム実装完了
- **2025年11月21日**: シーン遷移システム実装完了（FadeManager + ScenePortal）
- **2025年11月24日**: FadeManagerシステム完全作り直し、不要コード削除ルール追加（Unity.md）
