## シーン遷移後カメラ映像停止問題解決（2025-11-30）

### 問題
**症状**: シーン遷移後、Annotatable Screenのカメラ映像が静止画になる

**初回シーン**: カメラ映像が正常に動作
**シーン遷移後**: 静止画のまま更新されない

### 調査過程

#### 試行1: Stop()オーバーライド（失敗）
- **仮説**: PoseLandmarkerRunner.Stop()がSharedCameraImageSourceを停止
- **修正**: Stop()をオーバーライドしてImageSource.Stop()をスキップ
- **結果**: ❌ 効果なし

#### 試行2: SharedCameraImageSource再作成（失敗）
- **仮説**: シーン遷移後にImageSourceが正しく設定されていない
- **修正**: PoseLandmarkerRunner.Run()で毎回SharedCameraImageSourceを新規作成
- **結果**: ❌ 効果なし（ImageSource参照は正しかった）

#### 試行3: ログ追加で真相発見（成功）
- **調査**: CameraInput.Update()にログを追加
- **発見**: シーン遷移後に`webCamTexture.isPlaying=False`になっていた

**ログ比較**:
```
初回シーン:
[CameraInput] Update() - Frame updated. Count: 1200

シーン遷移後:
[CameraInput] Update() - Not updating: isInitialized=True, webCamTexture.isPlaying=False
```

### 根本原因
**WebCamTextureの停止**: シーン遷移時にWebCamTextureが停止し、CameraInput.Update()でのフレーム更新が止まっていた

**影響範囲**:
1. WebCamTextureが停止 → `webCamTexture.didUpdateThisFrame=false`
2. CameraInput.Update()で`currentFrame.SetPixels()`が実行されない
3. currentFrame（Texture2D）が最後のフレームのまま固定
4. Annotatable Screenに静止画が表示される

### 解決策

#### CameraInput.cs修正
```csharp
public void EnsureCameraIsPlaying()
{
    if (webCamTexture != null && !webCamTexture.isPlaying)
    {
        Debug.Log("[CameraInput] WebCamTexture was stopped, restarting...");
        webCamTexture.Play();
    }
}
```

#### PoseLandmarkerRunner.cs修正
```csharp
// シーン遷移後にWebCamTextureが停止している場合は再起動
cameraInput.EnsureCameraIsPlaying();

var sharedSource = new SharedCameraImageSource(cameraInput);
yield return sharedSource.Play();
```

### 動作確認結果
- ✅ シーン遷移後もカメラ映像が正常に動作
- ✅ WebCamTextureが自動的に再起動される
- ✅ Annotatable Screenのカメラ映像が更新され続ける

### 学び
- **DontDestroyOnLoad使用時の注意**: シーン遷移時にコンポーネントの状態が変化する可能性がある
- **WebCamTextureのライフサイクル**: シーン遷移時に自動的に停止することがある
- **デバッグログの重要性**: Update()に状態ログを追加することで真相が判明
- **仮説検証の重要性**: 複数の仮説を立てて順番に検証する

---

## 水流制御システム & FadeManager GameEvent統合（2025-11-28）

### WaterFlowController.cs作成
**目的**: EventListenerから水の流量（ConstantFlowRate）を制御

**機能**:
- `SetFlowRateHigh()` - フェード中の高速流量（1.0）
- `SetFlowRateLow()` - 通常時の低速流量（0.008）
- `SetFlowRateCustom(float)` - 任意の流量値に設定

**対象**:
- GameObject: `Dynamic Waves Object`
- Component: `KWS_DynamicWavesObject`
- Field: `ConstantFlowRate`

**使用方法**:
1. `Dynamic Waves Object`に`WaterFlowController.cs`をアタッチ
2. Inspector で `targetWaterObject` に自身を参照設定
3. EventListenerから`SetFlowRateLow()`等を呼び出し

---

### FadeManager.cs GameEvent統合
**目的**: FadeManagerの単一責任原則を保ちつつ、Fade待機完了時に任意のイベントを発火可能にする

**設計原則**:
- ✅ FadeManagerはFade演出のみに専念（Fade以外の処理を持たない）
- ✅ GameEvent（ScriptableObject）を使って疎結合
- ✅ 拡張性の確保（今後のFade時イベント需要に対応）

**追加フィールド**:
- `GameEvent onFadeInDelayComplete` - FadeIn待機完了時に発火
- `GameEvent onFadeOutDelayComplete` - FadeOut待機完了時に発火

**発火タイミング**:
- FadeIn(): delay待機完了 → **GameEvent発火** → 実際のフェード演出開始
- FadeOut(): delay待機完了 → **GameEvent発火** → 実際のフェード演出開始

**実装例（水流制御）**:
1. GameEventアセット作成（例：`OnFadeInDelayComplete.asset`）
2. FadeManagerのInspectorで`onFadeInDelayComplete`に設定
3. GameEventListenerを`Dynamic Waves Object`に配置
4. GameEventListenerのresponseに`WaterFlowController.SetFlowRateLow()`を設定

**結果**: FadeIn待機完了時に自動的に水流が低速（0.008）に切り替わる

---

## SetFieldValue改良 & ホラーゲーム風ライティング設定（2025-11-27）

### InspectorFieldSetterMCPTool.cs改良
**目的**: Volume ProfileのようなネストされたScriptableObjectフィールドに対応

#### ResolveNestedFieldPath()メソッド追加
**機能**:
- ドット記法対応（例：`profile.intensity.value`）
- 配列/リストインデックスアクセス対応（例：`components[0].fixedExposure`）
- 実際のランタイム型を使用（基底クラスではなく派生クラスの型）

**改良箇所**:
```csharp
// 配列/リスト要素取得時に実際のランタイム型を使用
nextType = nextObject != null ? nextObject.GetType() : nextType.GetElementType();
```

### ホラーゲーム風ライティング設定実装（完了）

#### 設定内容
**シーン**: RiverHoleTonnnelMap
**対象**: Main Camera > Volume > Main Camera Profile

#### **最終設定値**（全てのVolume Overrideを使用）:

**ColorAdjustments** (components[0]):
- `postExposure`: **-1.5**（暗闇演出、ポイントライト視認可能）
- `contrast`: **60**（高コントラスト）
- `saturation`: **-80**（モノクロ寄り）

**Vignette** (components[1]):
- `intensity`: **0.5**
- `smoothness`: **0.35**
- 画面周辺を暗くしてホラー感強調

**FilmGrain** (components[2]):
- `intensity`: **0.4**
- ホラー映画風の粒子感

**ChromaticAberration** (components[3]):
- `intensity`: **0.3**
- 色収差効果で不気味な雰囲気

#### 元の設定（バックアップ）
- `postExposure`: **-0.95**
- `contrast`: **28**
- `saturation`: **-53**

### 設定を元に戻す手順
```bash
# 元の暗いトンネル風設定に戻す
curl -X POST http://localhost:56780/mcp/ -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"SetFieldValue","arguments":{"objectName":"Main Camera","componentTypeName":"Volume","fieldName":"sharedProfile.components[0].postExposure.m_Value","value":"-0.95"}},"id":1}'

curl -X POST http://localhost:56780/mcp/ -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"SetFieldValue","arguments":{"objectName":"Main Camera","componentTypeName":"Volume","fieldName":"sharedProfile.components[0].contrast.m_Value","value":"28"}},"id":1}'

curl -X POST http://localhost:56780/mcp/ -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"SetFieldValue","arguments":{"objectName":"Main Camera","componentTypeName":"Volume","fieldName":"sharedProfile.components[0].saturation.m_Value","value":"-53"}},"id":1}'
```

### 動作確認結果
- ✅ SetFieldValueでVolume Profileのネストされたフィールドにアクセス可能
- ✅ 全Volume Override追加・設定完了（ColorAdjustments、Vignette、FilmGrain、ChromaticAberration）
- ✅ ホラーゲーム風設定をシーンに反映成功
- ✅ Play Modeテスト成功：ポイントライトはしっかり明るく、それ以外は暗闇
- ✅ リッチな質感達成（粒子感、色収差、周辺減光の複合効果）
- ✅ ユーザー確認済み：「とってもいい感じです　すばらしい」

---

## シーン遷移システム仕様変更 & 自動前進機能追加（2025-11-24）

### 仕様変更: 円環状一方通行シーン接続
**変更理由**: シーン接続が円環状一方通行のため、ポータルID・スポーン地点ID不要

#### ScenePortal.cs修正
- SpawnPoint[]配列削除 → **startPosition（固定1箇所）**
- Enter(string, string) → **Enter(string)**（targetSceneNameのみ）
- s_nextPortalID削除 → **s_shouldSpawnフラグ管理**
- CheckAndSpawnPlayer()シンプル化（ID判定なし）

#### RouteFollower.cs修正
- WaypointEvent拡張:
  - UnityEvent<string, string> → **UnityEvent<string>**
  - enableSceneTransition, targetSceneName追加
  - onSceneTransition追加（ScenePortal.Enter用）
- destinationPortalID削除

### 自動前進機能実装（テスト用）
**目的**: 腕振り不要でシーン遷移テスト可能に

#### RouteFollower.cs機能追加
- **autoMoveEnabled**フラグ追加（Inspector: Debug/Testing）
- Update()でautoMoveEnabled時に腕振り検知無視して強制前進

#### InspectorFieldSetterMCPTool.cs改良
- SetFieldValue()のprivate [SerializeField]対応（BindingFlags.NonPublic追加）

### 問題修正
1. **_FadeManager表示問題**:
   - RectTransform削除 → 空のGameObject再作成
   - Canvas/CanvasScaler/GraphicRaycaster削除不要（FadeManager.csが自動初期化）

2. **RouteFollowerモード問題**:
   - PingPong → **OneWay**に修正

3. **UnityEvent型不一致問題**:
   - UnityEvent（0パラメータ）→ UnityEvent<string>（1パラメータ）に修正
   - ScenePortal.Enter(string, string) → Enter(string)に修正

### 動作確認結果
- ✅ シーン遷移成功（SceneMoveTest → SceneMoveTest）
- ✅ スポーン位置配置成功（SpawnPoint_Start）
- ✅ 自動前進モード動作確認
- ⚠️ カメラ起動エラー（既存問題、自動前進モードでは無視可能）

---

## FadeManagerシステム完全作り直し（2025-11-24）

### 旧システムの問題点
1. **DontDestroyOnLoad使用**: シーン遷移後にCanvas/Imageが破壊される不具合
2. **Singleton設計**: 各シーンに配置する設計と相性が悪い
3. **タイミング制御の複雑さ**: 呼び出し側でFadeOut→SceneLoad→FadeInを管理
4. **小さな黒いキャンバス問題**: シーン遷移後に子のImageが消失

### 新システムの設計
1. **各シーンに配置**: DontDestroyOnLoad完全削除
2. **自動フェードイン**: シーン開始時に`SetFadeBlack()` → `FadeIn()`自動実行
3. **LoadSceneWithFade(string)**: FadeOut完了まで待機してからシーンロード
4. **IsFading静的プロパティ**: フェード中のプレイヤー停止（RouteFollower.Update()で判定）

### 実装内容
**FadeManager.cs**:
- `public static bool IsFading { get; private set; }` 追加
- `LoadSceneWithFade(string sceneName, float fadeOutDuration)` 実装
- `Awake()`: fadeImage未設定時に自動生成（Canvas+Image全画面黒パネル）
- `Start()`: autoFadeInOnStart=trueでシーン開始時に自動フェードイン
- Singleton削除、DontDestroyOnLoad削除

**ScenePortal.cs**:
- スポーン機能完全削除（startPosition, player, s_shouldSpawn, CheckAndSpawnPlayer()）
- `Enter(string targetSceneName)` - シーン遷移トリガーのみ
- FadeManagerに委譲

**RouteFollower.cs**:
- `Update()`冒頭で`if (FadeManager.IsFading) return;`追加
- フェード中はプレイヤーの移動・回転を完全停止

### セットアップ手順
**各シーンに配置**:
1. FadeCanvas作成（Canvas: ScreenSpaceOverlay, sortingOrder=9999）
2. FadeImage作成（子要素、黒色、RectTransform全画面）
3. FadeCanvasにFadeManagerコンポーネントアタッチ
4. FadeManager.fadeImageにFadeImageを参照設定

### 動作確認結果
- ✅ シーン遷移時に正しくフェードアウト→ロード実行
- ✅ 次シーンで自動的にフェードイン
- ✅ フェード中にプレイヤーの動き停止
- ✅ Canvas/Image破壊問題解決

### Unity.md更新
**🗑️ 不要コード・ゴミファイル削除の絶対原則**追加:
- 旧システム作り直し時は、コード+GameObject+Inspector参照を一括削除
- Grep検索で旧GameObject名を確認
- 完全にクリーンな状態にしてから「完了」と報告
- 放置は絶対禁止

---

## シーン遷移システム最適化（2025-11-26）

### 第1段階：ScenePortal純粋C#クラス化
**変更理由**: MonoBehaviourの継承が不要、動的インスタンス化でシンプル化

#### ScenePortal.cs
- MonoBehaviour継承削除 → **純粋なC#クラス**
- 無意味なコルーチン削除

#### RouteFollower.cs
- `UnityEvent<string> onSceneTransition`削除
- ScenePortalを`new`でインスタンス化して使用

### 第2段階：責任分離の適正化（同日追加）
**問題**: FadeManagerがシーン遷移処理まで担当していた（責任過多）

#### FadeManager.cs
- `LoadSceneWithFade()`削除
- **`FadeOutWithCallback()`追加** - フェード演出のみ担当
- シーン遷移の責任を完全に除去

#### ScenePortal.cs
- **シーン遷移の責任を担当**
- FadeManagerのコールバック方式を利用
```csharp
fadeManager.FadeOutWithCallback(() =>
{
    SceneManager.LoadScene(targetSceneName);
});
```

### 責任分離の結果
- **FadeManager**: フェード演出**のみ**
- **ScenePortal**: シーン遷移**のみ**（純粋C#クラス）
- 各クラスが単一責任原則を遵守

### 第3段階：演出完了待機問題の修正（同日追加）
**問題**: LookAt演出を待たずにシーン遷移が発生していた

#### RouteFollower.cs
- **AdvanceToNextWaypoint()修正** - 全ての処理を順序立てて実行
- **LookAtWithPause()改良** - コールバック引数追加
```csharp
// LookAt演出完了後に他の処理を実行
System.Action executeAfterEffects = () => {
    evt.onReached?.Invoke();  // 追加イベント
    // シーン遷移（最後に実行）
    if (evt.enableSceneTransition) {
        ScenePortal portal = new ScenePortal();
        portal.Enter(evt.targetSceneName);
    }
};
```

### 処理順序の保証
1. LookAt演出（存在する場合）
2. onReachedイベント
3. シーン遷移（最後）

コールバック方式により、演出完了を待ってから次の処理を実行

---

## 次回作業予定

### 拡張作業
1. Build Settingsにシーン追加
2. 複数シーン作成（Map1 ⇔ Map2 円環接続）
3. 各シーンの固定開始位置設定
4. 円環状一方通行遷移テスト

---

## プロジェクト構成情報

### カスタムMCPツール配置場所
**配置ディレクトリ**: `Assets/Scripts/MCPTools/`

**確認コマンド**:
```bash
cd "C:\Users\Simna\KWS2 DynamicWaterAsset" && find Assets/Scripts/MCPTools -name "*MCPTool*.cs" 2>/dev/null
```

**登録済みカスタムMCPツール数**: 27個

**主要ツール**:
- AllMCPToolsBuilder.cs - 全ツール登録管理
- InspectorFieldSetterMCPTool.cs - SetFieldValue（Volume Profile操作等）
- RefreshMCPToolsMCPTool.cs - 完全自動化ツール
- ComponentAttachMCPTool.cs, GameObjectManagerMCPTool.cs等

---

## 参考リソース

- YOLOv8 ONNX Export: https://docs.ultralytics.com/modes/export/
- ONNX Runtime: https://github.com/microsoft/onnxruntime
- KWS Water System: https://kripto289.gitbook.io/kripto289-docs
- MediaPipeUnityPlugin: https://github.com/homuler/MediaPipeUnityPlugin
- Unity WebCamTexture: https://docs.unity3d.com/ScriptReference/WebCamTexture.html

---

## 開発履歴（要約）

- **2025年10月18日**: 人物検知システム移植完了
- **2025年10月21日**: イベントシステム実装完了
- **2025年11月1日**: 腕振り歩行検知システム実装完了、MediaPipe統合完了
- **2025年11月4-5日**: ルート移動システム実装完了、カメラ注視演出実装完了、カメラ統合システム実装完了
- **2025年11月21日**: シーン遷移システム実装完了（FadeManager + ScenePortal）
- **2025年11月24日**: FadeManagerシステム完全作り直し、不要コード削除ルール追加（Unity.md）
- **2025年11月27日**: InspectorFieldSetterMCPTool.cs改良（ネストされたScriptableObject対応）、ホラーゲーム風ライティング設定完了（全Volume Override使用）